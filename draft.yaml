models:
  AnnounceQueue:
    id: id
    title: string nullable
    text: longtext nullable
    images: json nullable
    need_send_at: timestamp nullable
    sent_at: timestamp nullable
    softDeletes
    timestamps



controllers:
  AnnounceQueue:
    resource: api

