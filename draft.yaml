models:
  Message:
    id: id
    full_name: string nullable
    sms: longtext nullable
    identify: string nullable
    h_user_id: integer nullable
    created_at: timestamp
    updated_at: timestamp

controllers:
  Message:
    resource: api

