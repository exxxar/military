models:
   AidCenter:
    id: id
    city: string nullable
    region: string nullable
    address: string:400
    phone: string:100
    required: longtext nullable
    description: longtext nullable

seeders: AidCenter

controllers:
  AidCenter:
    resource: api

